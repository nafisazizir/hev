# kev: what it is, what we reuse, what we don't

Local clone: `/Users/nafis/Documents/personal/kev` at commit `20fa6268c8ceb226530be2fb5266ab2c36b37724` (2026-09-19 22:35 EDT; refreshed 2026-09-20 from the `cc954f2` pin this file was written against). Upstream: github.com/jaredpalmer/kev, Apache-2.0. Read this before porting anything. Sections up to "What kev deferred" describe the cc954f2 state that Hev's M0-M3 were built on and are still accurate at HEAD unless the dated section at the end says otherwise.

## One-paragraph summary

kev is a LoRA adapter (r=16) plus a 256-dim pointer head on Qwen 0.5B/0.6B. A request's state and all its questions are packed into one sequence; a block-causal mask lets each question read the state but not sibling questions; a `<decide>` token at the end of each question is dotted against each option's `</opt>` vector; softmax gives the answer. Trained with cross-entropy on about 13k records from public classification datasets. The model file is 134 lines. The other ~2,800 lines are evaluation discipline.

## File map

| kev file | What | Hev status |
|---|---|---|
| `kev/model.py` | encode, block-causal mask, PointerHead, DecisionModel; at HEAD also `option_isolation`, `special_embeddings`, `head_dim`, `lora_targets`, `dtype` knobs | **Rewritten** in `hev/model.py` with option-level isolation. Same delimiters and limits. Separately, **vendored verbatim** (HEAD 20fa626, sha256 `839dd7632ec291ac4740035c86f780f8f13da2b74b54a146ce90ff0ce8de527b`) as `hev/kev_model.py`, inference only, so released kev checkpoints can be scored under kev's own packing; see the dated section below. |
| `kev/api.py` | TypeSafe schema, render, to_record, to_answers | **Copied** to `hev/api.py`, `qtype` added to records. |
| `kev/data.py` | dataset conversion, augment, none_pair, materialize, source policy | **Partly ported** to `hev/data.py`: materialize, augment, none_pair, policy. Dataset download/build not ported. |
| `kev/suite.py` | freeze suites, load_split with checksums, contrast_cases | **load_split ported** to `hev/suite.py`. freeze and contrast_cases not ported. |
| `kev/train.py` | training loop, perm-KL, ranked probability score, batching | Not ported. M1 writes Hev's own; reuse the loss definitions. |
| `kev/evaluate.py` | accuracy/ECE/permutation/IIA/isolation/latency studies | Metric functions ported at M1; at M2 the clean/variant row and metric summaries are adapted into `hev/evaluate.py` with attribution. Model inference and orchestration remain Hev-specific. kev's checkpoint loading (`kev/evaluate.py:40-54`: fp32, PEFT adapter, PointerHead from `head.pt`) is reproduced by `hev/kev.py` (`KevPredictor`) on top of the vendored `hev/kev_model.py`, so a kev checkpoint runs through Hev's evaluator without kev's orchestration. |
| `kev/benchmark.py`, `kev/compare.py`, `kev/plot.py` | bootstrap CIs, kev-vs-jev comparison, figures | Grouped bootstrap logic adapted into `hev/evaluate.py`/`hev/compare.py` at M2 with attribution. Kev v2 aggregate result artifacts lack per-example rows, so kev seed-0/seed-1 numbers enter only as hash-verified point baselines; no paired Hev-vs-kev CI is possible on kev's artifacts. With the vendored inference copy (`hev/kev_model.py` + `hev/kev.py`) a kev checkpoint can be re-scored row by row inside Hev, which is what makes a paired CI possible on v4. Plotting not ported. |
| `kev/jev.py` | client for TypeSafe's Jev via AI Gateway | **Rewritten** at M3 as `hev/jev.py` against TypeSafe's direct API, with immutable calibration/development ledgers, bounded cost/calls, rounded-probability normalization, and version capture. |
| `kev/serve.py` | FastAPI `/v1/systemone`, `/permute`, `/separate` | M3 writes Hev's own `/v1/systemone` and `/v1/models` compatibility routes; playground-only probe routes are not ported. |
| `kev/experiment.py`, `modal_app.py` | config-only trial runner, Modal H100 | Port if MPS becomes the bottleneck. |
| `kev/composition.py`, `kev/study_v3.py`, `kev/contrastive.py` | synthetic compositional policy data, v3 study | Not needed until M4. |
| `playground/` | Next.js UI with isolation/forgery probes and chess | Works against Hev unchanged once M3 serve exists. |
| `tests/` | unit, api, research, v3 tests | Read `test_unit.py` for what kev asserts; Hev's tests are a superset for the mask. |

## Baseline numbers (quote with these paths)

All from the kev clone.

- Historical, not comparable to the 0.6B numbers below: released kev-0.5b (Qwen2.5-0.5B, decision-v1-era data), held-out in-distribution: ECE 0.065 raw, 0.031 after temperature; 7% argmax flips and p90 spread 0.25 under option reordering. `MODEL_CARD.md`. The order-sensitivity figure for the comparable Qwen3-0.6B-Base checkpoint is in the v4 lines below.
- kev vs Jev, familiar sources: 79.7% vs 81.1% micro accuracy, macro difference -1.8 [-5.5, +1.7]. `runs/kev-vs-jev-v1.json`.
- kev vs Jev, transfer-v1: 63.3% vs 82.3%, -19.1 [-23.1, -15.0]. `runs/kev-vs-jev-transfer-v1.json`.
- Qwen3-0.6B-Base on the v2 recipe, decision-v2 development: 81.6% / 79.3% (two seeds); transfer-v2: 62.0% / 62.1%. `runs/ablation-v2/06-trial-6/result.json`, `07-trial-7/result.json`.
- M3 same-v2 aggregate: Hev Pointer 80.00% / 60.71% two-seed means, kev 80.46% / 62.05%, hosted Jev `1.13.0` 83.50% / 85.36% on decision / transfer development. Jev is one hosted snapshot and its rounded-zero NLL is floor-sensitive. `runs/m3-three-way-v2-r1/result.json`.
- Qwen2.5-0.5B same recipe: 74.2% / 65.8% dev; 60.5% / 48.2% transfer. `runs/ablation-v2/results.jsonl`.
- v3 capacity study: Qwen3-4B beats 0.6B by +17.5 pp on transfer-v3 [+13.0, +22.1]. `runs/v3-data-capacity-s0/results.jsonl`.
- Calibration warning: one 0.6B trial scored 50% on transfer authorization with 99.6% mean confidence. `runs/ablation-v2/06-trial-6/result.json`.
- Qwen3-0.6B-Base on decision-v4 / transfer-v4 development (the `jaredpalmer/kev-0.6b` preview recipe, plain kev encoding), three seeds: dev 0.8046 / 0.7927 / 0.7998, transfer 0.5976 / 0.5945 / 0.6052 (seeds 0 / 1 / 2). `runs/v4-06b-hardened/results.jsonl` trials `00-trial-0`, `01-trial-1`, `02-trial-2` (`clean.acc`, `transfer_clean.acc`).
- Order sensitivity of that seed-0 checkpoint on decision-v4 development: argmax flip rate 0.0167, mean max probability delta 0.0117 over 60 permutation cases. `runs/v4-06b-hardened/00-trial-0/result.json` (`permutation`). On the locked read: flip rate 0.0278, mean max delta 0.0260 over 72 cases. `runs/locked/kev-06b-preview-ungated/summary.json` (`suites.decision.permutation`).
- Same seed-0 checkpoint, one ungated locked-test read: decision 0.8192 (n 1200 clean questions), transfer 0.6311 (n 656), evaluated on mps fp32. `runs/locked/kev-06b-preview-ungated/summary.json`.
- Qwen3-0.6B-Base with kev's `option_isolation=1` on decision-v4 / transfer-v4 development: dev 0.7998, transfer 0.5808 (`runs/arch-06b/results.jsonl` trial `00-trial-0`); with isolation plus trained delimiter embeddings: dev 0.7991, transfer 0.5991 (`03-trial-3`). Permutation flip rate 0.0 with mean max delta 1.29e-7 (trial 00) and 7.18e-8 (trial 03). `runs/arch-06b/00-trial-0/result.json`, `runs/arch-06b/03-trial-3/result.json` (`permutation`).
- Jev on decision-v4 development: acc 0.845, Brier 0.237; on transfer-v4 development: acc 0.857. kev `PLAN.md:98`.

## Kev's v2 training recipe (what M2 reproduces)

From `PLAN.md` and `train.py` defaults: Qwen3-0.6B-Base pinned to the suite revision, LoRA r=16 alpha=32 dropout 0.05 on all attention and MLP projections, lr 2e-4 OneCycle with 10% warmup, AdamW wd 0.01, grad clip 1.0, effective batch 8 records, 2 epochs, fp32 on MPS, augmentation p_none 0.1 / p_none_distract 0.12 / p_distract 0.15, fresh permutation each epoch. Training on decision-v2 `train.jsonl` (3,432 records). Temperature fit on `calibration.jsonl` (448) only.

## Things kev learned the hard way (do not repeat)

- "None of the above" with a single wording becomes a shortcut. Vary wording and use it as both right and wrong answer. (`data.py` NONE_OPTIONS comment.)
- Minimal pairs must share sentence order and option order; change one fact only. (`PLAN.md` v3 corrections.)
- Compare semantic answer keys, not option positions, when scoring pairs.
- Low aggregate ECE on one transfer set does not mean calibration transfers.
- Zero argmax flips observed in Jev do not prove Jev's architecture is order-invariant; its probabilities do move.
- Eager attention on MPS, SDPA on CUDA; the float 4D mask is only known-good on eager for MPS.
- Evaluate in fp32 with TF32 off on CUDA for exactness.

## What kev deferred that Hev picks up

`PLAN.md` "Status and deferred work", last item: "Deferred: option-order architecture experiments. Do not infer Jev's architecture from zero argmax flips." That item is Hev. At HEAD 20fa626 the line is still present and unchecked (`PLAN.md:291`) even though kev has since shipped `--option_isolation` as a training-time switch and measured it at 0.6B and 4B; see below. What kev did not do is make invariance the default, pair it with a permutation-equivariant readout, or study it as the object of interest rather than one knob in a hill-climb, so the item remains Hev's.

## What changed in kev after cc954f2 (62 commits, 2026-09-18 19:50 to 2026-09-19 22:35 EDT; written 2026-09-20)

`git rev-list --count cc954f2..20fa626` is 62; by UTC day the commits fall 2 / 57 / 3 on 09-18 / 09-19 / 09-20, the first at 2026-09-18 19:50 -0400 (`cbb67c7`, "Autoresearch harness ... architecture switches (option isolation = exact permutation invariance ...)") and the last at 2026-09-19 22:35 -0400 (`20fa626`, "v7/v8 final results; kev-4b and kev-8b previews updated on the Hub"). New files: `kev/autoresearch.py` (bounded hill-climb, leaderboard, spend ledger), `kev/anchors.py` (frozen-base zero-shot distributions for an anchoring loss, `kev/train.py` `--anchor` / `--anchor_w`), `scripts/base_mmlu_probe.py`, `evals/v5` through `evals/v8`, `evals/public-pool-v5/v6`, `docs/model-cards/`. Files Hev copied from cc954f2 are unchanged except where (e) says.

**(a) kev now has an `--option_isolation` training flag.** `kev/train.py:67`: `--option_isolation {0,1}`, default 0, help "option spans are isolated sub-branches with shared positions (exact permutation invariance)". Implementation in `kev/model.py`: `encode(..., option_isolation=False)` (`kev/model.py:30`) gives every option span the same starting position and puts `<decide>` at one fixed position after the longest span (`kev/model.py:56-58`); `branch_mask_batch` takes an `opts` array and, inside a question, lets an option-span token attend only to state, instruction and its own span while `<decide>` attends to everything in the question (`kev/model.py:82-83`, mask rule `kev/model.py:93-99`); `DecisionModel(option_isolation=...)` records the setting (`kev/model.py:116,124`), `encode_record` applies it (`kev/model.py:137-138`) and a batch may not mix isolated and plain encodings (`kev/model.py:151-153`). kev `PLAN.md:93-94` describes it as: "`option_isolation` (option spans are isolated sub-branches with shared positions; permutation invariance exact by construction, 1.2e-7 measured on the real model)". This is the same mask idea as Hev's, arrived at independently in kev as an opt-in knob; the differences that remain are listed under (f).

**(b) kev's 0.6B findings on v4.** Option isolation trains normally at 0.6B. `runs/arch-06b/results.jsonl` trial `00-trial-0` (config `option_isolation: 1`, everything else the hardened recipe): dev 0.7998, transfer 0.5808; trial `03-trial-3` (`option_isolation: 1` plus `special_embeddings: 1`): dev 0.7991, transfer 0.5991. Measured flip rate is exactly 0.0 on both the decision (n 60) and transfer (n 36) permutation studies, with mean max probability delta 1.29e-7 / 5.09e-7 for trial 00 and 7.18e-8 / 6.74e-7 for trial 03 (`runs/arch-06b/00-trial-0/result.json`, `runs/arch-06b/03-trial-3/result.json`, key `permutation` and `transfer.permutation`). The plain-encoding trials in the same study land at dev 0.7943-0.8125 and transfer 0.5899-0.6098 (`results.jsonl` trials 01, 02, 05, 06), so kev's `PLAN.md:115-116` reads it as parity: "Option isolation trains normally at 0.6B (dev 0.800) with a measured flip rate of exactly 0.0; transfer 0.58-0.60 (parity with the plain encoding; six salvaged arch-screen trials all within noise of the incumbent)". Only six of the eight arch-screen trials exist because Modal hit its spend limit mid-study (`PLAN.md:104-107`); trials 04 and 07 are absent from `results.jsonl`. At 4B with the low learning rate, isolation cost accuracy: `PLAN.md:176`: "Round auto-4b-r1 (...): all 0.755-0.767; option isolation at low lr 0.729 (-5.8 pp, significant) - isolation costs accuracy at 4B." The run is `runs/auto-4b-r1/00-trial-0` (Qwen3-4B-Base, lr 3e-5, `option_isolation: 1`): transfer 0.7287, dev 0.841, flip rate 0.0 with mean max delta 2.24e-7 (`runs/auto-4b-r1/results.jsonl`, `runs/auto-4b-r1/00-trial-0/result.json`). The plain-encoding trials of that round are at transfer 0.7546-0.7668 (`results.jsonl` trials 01-05), a 2.6-3.8 pp gap; the -5.8 pp figure is `PLAN.md`'s own paired statement against its low-lr incumbent and is quoted as such. `PLAN.md:168` files isolation under "What did not work: ... option isolation (exact permutation invariance at no accuracy cost, but no accuracy gain)". Hev's claim is invariance, not accuracy, so this is the baseline to beat on equal footing, not a refutation.

**(c) The published `jaredpalmer/kev-0.6b` research preview.** It is trial `v4-06b-hardened/00-trial-0`, seed 0 of 3 (`runs/locked/kev-06b-preview-ungated/summary.json`, field `trial`; `PLAN.md:150-152`; kev `README.md:52`). Trained on decision-v4 (suite sha256 `1b33e566d114f9eafeff55b36c221fadb2a4ae358a1b9cc68006e82c7cfad8f1`): 10,000 public records at 1,000 per source from `evals/public-pool-v4` plus 896 programmatic policy records (448 `legacy_policy` + 448 `compositional`; `evals/v4/decision-v4/manifest.json` `protocol` and `files.train.jsonl`, 10,896 records). Config (`runs/v4-06b-hardened/00-trial-0/provenance.json`): Qwen3-0.6B-Base, 2 epochs, lr 2e-4, LoRA 16, batch 8, accum 1, p_none 0.1, p_none_distract 0.12, p_distract 0.15, `p_none_pair` 0.25, bf16, on an NVIDIA H100 80GB (`gpu`), kev commit `6cfa03ab`; 21,792 records requested over the two epochs, 2,724 optimizer steps, 613 s wall (`result.json` `training_resources`). Development: dev 0.8046 / transfer 0.5976 (`runs/v4-06b-hardened/results.jsonl` trial 00; kev rounds this to 0.805/0.598). Seeds 1 and 2 of the same recipe: 0.7927 / 0.5945 and 0.7998 / 0.6052 (`results.jsonl` trials 01 and 02). One exploratory, ungated locked read (the trial fails the 70% held-out-pair gate; `summary.json` field `read`): decision 0.8192 (n 1200), transfer 0.6311 (n 656), `eval_device` "mps (fp32)". Per-task locked numbers are in the same file. Its permutation flip rate is not zero (0.0167 dev, 0.0278 locked; paths in "Baseline numbers"), which is the number Hev's exact invariance is set against.

**(d) v4 through v8 share development and test bytes.** `shasum -a 256` over the kev clone: `decision-v4..v8/development.jsonl` are all `8d5765d7…`, `test.jsonl` all `cd7d129a…` (the v2 test bytes); `transfer-v4/v5/v6` development `ff374c49…` and test `c30a9127…`. `evals/v7` and `evals/v8` contain only a decision suite and are scored against `transfer-v4` (`PLAN.md:212-213`). Later versions change the training partition only: v5 23,584 records (20k public), v6 13,896 (adds ARC-Challenge, OpenBookQA, CommonsenseQA as trainable sources), v7 12,576 (four ordinal Score policy families, 60 random rule trees), v8 12,688 (`evals/v5..v8/decision-v*/manifest.json` `files.train.jsonl`; `PLAN.md:212-216`). They target Qwen3-4B and 8B (`kev-4b`, `kev-8b`; `PLAN.md:146-148`, `README.md:214`). Hev copied v4 only; any kev number on v5-v8 development or test is directly comparable to a Hev number on v4.

**(e) Rendering parity holds.** `git diff --stat cc954f2..20fa626 -- kev/api.py` is empty, and the `code_hashes.api.py` in both the v2 and v4 manifests is `cdb0602684d798ccd3fc2c86be622f2a8e7bcdeee64d07f95604c7d3fd4701ec`. `kev/data.py` changed by +40 lines: new knowledge-MCQ builders `_mcq`, `_arc`, `_openbookqa`, `_csqa` with `arc`, `openbookqa`, `csqa` added to `TRAINABLE` (none of them appear in v4), and `materialize` now also writes `qid` and `keys` onto each question (`kev/data.py` diff, `materialize` tail). Neither touches rendered text, so `hev/api.py` and `hev/data.materialize` still produce the same bytes kev fed its models; `hev/kev_model.py`'s docstring restates this. `kev/suite.py` gained `fetch_partition` (+21 lines, Hub mirror `jaredpalmer/kev-suites`), which is why `decision-v4/train.jsonl` is not in git (`evals/README.md`).

**(f) kev's `head.pt` versus Hev's `readout.pt`, and why Hev vendors `hev/kev_model.py`.** kev saves (`kev/train.py:215-217`) `{"head": PointerHead state_dict, "base", "base_revision", "lora", "head_dim", "option_isolation", "special_embeddings", "holdout", "args", "suite_sha256"}` next to the PEFT adapter and tokenizer; the published 0.6B was trained before the last five keys existed, so an old `head.pt` resolves to `head_dim` 256, `option_isolation` False, `special_embeddings` False. Hev saves (`hev/train.py:313-322`) `{"readout", "base", "base_revision", "lora", "suite_sha256", "args", "training_config_sha256", "provenance"}`. The schemas are not interchangeable, and more importantly the packing is not: kev's `encode` (`kev/model.py:30-66`) puts `<decide>` after the option spans with sequential positions, whereas Hev's `hev/model.py` puts `<decide>` before the options, gives every option the same start position and always isolates option spans. A kev checkpoint scored under Hev's packing would see a sequence it was never trained on. So Hev keeps a verbatim copy of `kev/model.py` at 20fa626 as `hev/kev_model.py` (created by another agent in this session; sha256 of the upstream file `839dd7632ec291ac4740035c86f780f8f13da2b74b54a146ce90ff0ce8de527b`, verified by `tests/test_kev.py`; attribution in `THIRD_PARTY_NOTICES.md`), and `hev/kev.py` wraps it as `KevPredictor` behind the same contract as `hev.evaluate.LocalPredictor`. It is inference-only: Hev does not train with it, and it is the only kev code copied wholesale, permitted by the "port pieces when a milestone needs them" rule because the alternative is quoting kev's aggregate numbers without a paired comparison.
