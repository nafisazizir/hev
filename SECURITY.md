# Security

hev is a research prototype. The included server has no authentication and is intended for local use only. It binds to `127.0.0.1` by default; do not expose it directly to an untrusted network.

Report vulnerabilities through GitHub's private security advisory flow for `nafisazizir/hev`. Do not include secrets, private data or exploit payloads in a public issue.
